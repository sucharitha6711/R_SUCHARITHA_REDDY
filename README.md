📜 Description of Each Problem
Problem 1 – Calculator

Implements a simple calculator using a class.
Accepts inputs a, b (double) and the operation type (add, sub, mul, div).

Problem 2 – Odd Number Series

For an input a, generates a sequence:
1, 3, 5, ... until the ath odd number.

Problem 3 – Conditional Odd Number Pattern

If input is odd, print odd numbers until that odd limit.

If input is even, print odd numbers until the previous odd number.

Problem 4 – Count Multiples of 1 to 9

Given a list of integers, count how many numbers are divisible by each number from 1 to 9.
Outputs a dictionary format.
